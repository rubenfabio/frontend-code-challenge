"use client";
import styled from "styled-components";

const Button = styled.button`
  color: ${(p) => p.theme.palette.common.white};
  background-color: ${(p) => p.theme.palette.common.brand};
  display: inline;
  text-transform: uppercase;
  text-align: center;
`;
